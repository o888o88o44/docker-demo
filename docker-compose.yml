version: "3.8"

services:
  app:
    image: my-java-app
    ports:
      - "8081:8080"
    depends_on:
      - db

  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: "123456"
      MYSQL_DATABASE: testdb
    ports:
      - "3307:3306"